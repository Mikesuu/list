name: 自动更新策略路由列表

on:
  workflow_dispatch:  
  schedule:
    # 每天 UTC 时间 00:00 运行一次
    - cron: '0 0 * * *' 
  push:
    paths:
      - 'src/**' 

jobs:
  generate_mikrotik_list:
    runs-on: ubuntu-latest
    
    # 关键修复：授予写入权限，解决 403 错误
    permissions:
      contents: write 
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          
      - name: 设置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'
          
      - name: 安装依赖
        run: pip install requests 

      - name: 运行 Python 脚本生成 RSC 文件
        run: python src/domain_processor.py

      - name: 检查并提交更改
        id: commit_check
        run: |
          git status
          if [ -n "$(git status --porcelain)" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi
          
      - name: 提交更新到仓库
        if: steps.commit_check.outputs.has_changes == 'true'
        run: |
          git config --global user.name 'GitHub Actions Bot'
          git config --global user.email 'actions@github.com'
          git add fwd-ip-list.rsc
          git commit -m "🤖 自动更新策略路由 IP 列表"
          git push
